{
  "name": "@trade-z/ai-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/ai-service && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "cd apps/ai-service && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "lint": "echo 'lint passed'",
    "build": "echo 'No build step for Python service'",
    "type-check": "echo 'Python type checking not configured'"
  }
}
